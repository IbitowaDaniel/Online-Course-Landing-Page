

export const SectionHeader = ({ heading, headingSecondary }) => (
  <div className="text-center">
    <p className="heading">{heading}</p>
    <h3 className="headingSecondary">{headingSecondary}</h3>
  </div>
);